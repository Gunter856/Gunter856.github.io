name: index.html

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Validate HTML
        uses: jakejarvis/html-validate-action@v2
        with:
          path: ./index.html
          config: ./html-validate.json  # Replace wit
